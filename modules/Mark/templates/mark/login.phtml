<?php
/**
 * HDM Boot Protocol - Mark Login Template
 * 
 * Secure login form for mark users with modern design
 */
?>

<!DOCTYPE html>
<html lang="sk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= $this->escapeHtml($title ?? 'Mark Login') ?> - HDM Boot Protocol</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom Mark Login Styles -->
    <style>
        body {
            background: linear-gradient(135deg, #dc3545 0%, #6f42c1 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .mark-login-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 3rem;
            width: 100%;
            max-width: 450px;
            backdrop-filter: blur(10px);
        }
        
        .mark-logo {
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .mark-logo h1 {
            color: #dc3545;
            font-weight: 700;
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }
        
        .mark-logo p {
            color: #6c757d;
            font-size: 1rem;
            margin: 0;
        }
        
        .form-floating {
            margin-bottom: 1.5rem;
        }
        
        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        
        .form-control:focus {
            border-color: #dc3545;
            box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25);
        }
        
        .btn-mark {
            background: linear-gradient(135deg, #dc3545 0%, #6f42c1 100%);
            border: none;
            border-radius: 12px;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            color: white;
            width: 100%;
            transition: all 0.3s ease;
        }
        
        .btn-mark:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(220, 53, 69, 0.3);
            color: white;
        }
        
        .alert {
            border-radius: 12px;
            border: none;
            margin-bottom: 1.5rem;
        }
        
        .mark-footer {
            text-align: center;
            margin-top: 2rem;
            color: #6c757d;
            font-size: 0.9rem;
        }
        
        .mark-footer a {
            color: #dc3545;
            text-decoration: none;
        }
        
        .mark-footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="mark-login-container">
        <div class="mark-logo">
            <h1>🔒 MARK</h1>
            <p>HDM Boot Protocol</p>
        </div>
        
        <?php if (isset($error) && $error): ?>
            <div class="alert alert-danger" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i>
                <?= $this->escapeHtml($error) ?>
            </div>
        <?php endif; ?>
        
        <form method="post" action="/mark/login">
            <div class="form-floating">
                <input type="text" 
                       class="form-control" 
                       id="username" 
                       name="username" 
                       placeholder="Username"
                       required
                       autocomplete="username">
                <label for="username">Username</label>
            </div>
            
            <div class="form-floating">
                <input type="password" 
                       class="form-control" 
                       id="password" 
                       name="password" 
                       placeholder="Password"
                       required
                       autocomplete="current-password">
                <label for="password">Password</label>
            </div>
            
            <div class="d-grid">
                <button type="submit" class="btn btn-mark">
                    <i class="bi bi-shield-lock"></i>
                    Secure Login
                </button>
            </div>
        </form>
        
        <div class="mark-footer">
            <p>
                <strong>Mark Access Only</strong><br>
                <a href="/">← Back to Main Site</a>
            </p>
        </div>
    </div>
    
    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</body>
</html>
