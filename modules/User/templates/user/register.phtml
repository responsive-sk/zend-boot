<!DOCTYPE html>
<html lang="sk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= $this->escapeHtml($title ?? 'Register') ?> - User Module</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .register-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .register-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .register-header {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 2rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                    <div class="register-card">
                        <div class="register-header">
                            <h2 class="mb-0">
                                <i class="fas fa-user-plus me-2"></i>
                                Vytvoriť Účet
                            </h2>
                            <p class="mb-0 mt-2 opacity-75">Pridajte sa k nám! Vytvorte si účet a začnite.</p>
                        </div>
                        
                        <div class="card-body p-4">
                            <?php if ($error): ?>
                                <div class="alert alert-danger" role="alert">
                                    <i class="fas fa-exclamation-triangle me-2"></i>
                                    <?= $this->escapeHtml($error) ?>
                                </div>
                            <?php endif; ?>

                            <?php if ($success): ?>
                                <div class="alert alert-success" role="alert">
                                    <i class="fas fa-check-circle me-2"></i>
                                    <?= $this->escapeHtml($success) ?>
                                </div>
                            <?php endif; ?>

                            <?php
                            $form->prepare();
                            echo $this->form()->openTag($form->setAttribute('class', 'needs-validation')->setAttribute('novalidate', true));
                            ?>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <?= $this->formLabel($form->get('username')->setLabelAttributes(['class' => 'form-label'])) ?>
                                    <?= $this->formElement($form->get('username')) ?>
                                    <?= $this->formElementErrors($form->get('username'), ['class' => 'invalid-feedback d-block']) ?>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <?= $this->formLabel($form->get('email')->setLabelAttributes(['class' => 'form-label'])) ?>
                                    <?= $this->formElement($form->get('email')) ?>
                                    <?= $this->formElementErrors($form->get('email'), ['class' => 'invalid-feedback d-block']) ?>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <?= $this->formLabel($form->get('password')->setLabelAttributes(['class' => 'form-label'])) ?>
                                    <?= $this->formElement($form->get('password')) ?>
                                    <?= $this->formElementErrors($form->get('password'), ['class' => 'invalid-feedback d-block']) ?>
                                    <div class="form-text">
                                        <small>Password must contain at least 8 characters with uppercase, lowercase, and number.</small>
                                    </div>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <?= $this->formLabel($form->get('password_confirm')->setLabelAttributes(['class' => 'form-label'])) ?>
                                    <?= $this->formElement($form->get('password_confirm')) ?>
                                    <?= $this->formElementErrors($form->get('password_confirm'), ['class' => 'invalid-feedback d-block']) ?>
                                </div>
                            </div>

                            <div class="mb-3 form-check">
                                <?= $this->formElement($form->get('accept_terms')) ?>
                                <?= $this->formLabel($form->get('accept_terms')->setLabelAttributes(['class' => 'form-check-label'])) ?>
                                <?= $this->formElementErrors($form->get('accept_terms'), ['class' => 'invalid-feedback d-block']) ?>
                            </div>

                            <?= $this->formElement($form->get('csrf_token')) ?>

                            <div class="d-grid">
                                <?= $this->formElement($form->get('submit')) ?>
                            </div>

                            <?= $this->form()->closeTag() ?>

                            <hr class="my-4">
                            
                            <div class="text-center">
                                <p class="mb-0">
                                    Už máte účet?
                                    <a href="/user/login" class="text-decoration-none">Prihláste sa tu</a>
                                </p>
                                <p class="mt-2 mb-0">
                                    <a href="/" class="text-muted text-decoration-none">
                                        <i class="fas fa-arrow-left me-1"></i>
                                        Späť na Domov
                                    </a>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
</body>
</html>
