<!DOCTYPE html>
<html lang="sk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= $this->escapeHtml($title ?? 'Dashboard') ?> - User Module</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">User Dashboard</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/user/logout">Odhlásiť</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">Vitajte, <?= $this->escapeHtml($username) ?>!</h4>
                    </div>
                    <div class="card-body">
                        <?php if ($flash_success): ?>
                            <div class="alert alert-success">
                                <?= $this->escapeHtml($flash_success) ?>
                            </div>
                        <?php endif; ?>

                        <p>Úspešne ste sa prihlásili do svojho dashboardu.</p>

                        <h5>Vaše Údaje:</h5>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">
                                <strong>Používateľské meno:</strong> <?= $this->escapeHtml($username) ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Email:</strong> <?= $this->escapeHtml($userDetails['email'] ?? 'N/A') ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Role:</strong>
                                <?php foreach ($roles as $role): ?>
                                    <span class="badge bg-secondary me-1"><?= $this->escapeHtml($role) ?></span>
                                <?php endforeach; ?>
                            </li>
                            <li class="list-group-item">
                                <strong>ID Používateľa:</strong> <?= $this->escapeHtml($userDetails['id'] ?? 'N/A') ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Účet Vytvorený:</strong> <?= $this->escapeHtml($userDetails['createdAt'] ?? 'N/A') ?>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Rýchle Akcie</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/" class="btn btn-outline-primary">Domovská Stránka</a>
                            <?php if (in_array('admin', $roles)): ?>
                                <a href="/user/admin" class="btn btn-outline-warning">Admin Panel</a>
                            <?php endif; ?>
                            <a href="/user/logout" class="btn btn-outline-danger">Odhlásiť</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
