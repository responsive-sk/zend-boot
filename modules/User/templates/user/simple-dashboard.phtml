<!DOCTYPE html>
<html lang="sk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= $escapeHtml($title ?? 'Dashboard') ?></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Simple Dashboard</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/simple-logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">Welcome, <?= $escapeHtml($username) ?>!</h4>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-success">
                            ✅ Login successful! You are now authenticated.
                        </div>
                        
                        <h5>Your Session Details:</h5>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">
                                <strong>User ID:</strong> <?= $escapeHtml($user_id) ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Username:</strong> <?= $escapeHtml($username) ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Roles:</strong> 
                                <?php foreach ($roles as $role): ?>
                                    <span class="badge bg-secondary me-1"><?= $escapeHtml($role) ?></span>
                                <?php endforeach; ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Login Time:</strong> <?= $login_time ? date('Y-m-d H:i:s', $login_time) : 'N/A' ?>
                            </li>
                            <li class="list-group-item">
                                <strong>Session ID:</strong> <?= session_id() ?>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Quick Actions</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/" class="btn btn-outline-primary">Home Page</a>
                            <a href="/debug" class="btn btn-outline-info">Debug Info</a>
                            <a href="/simple-logout" class="btn btn-outline-danger">Logout</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
